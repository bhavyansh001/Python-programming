# The name of the file is such because I first named it webbrowser.py, it caused a looping error so the name of the file cannot be the module you are importing
import webbrowser

url = "google.com"
webbrowser.open(url)